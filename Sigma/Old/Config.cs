namespace SigmaOld
{
    /// <summary>
    /// A class holding all the default configuration values for Sigma. If Sigma
    /// is meta-built (built by itself), these values are overriden and this class
    /// is discarded. Greater and easier control over these values are provided
    /// by meta-building.
    /// </summary>
    public class Config
    {
        /// <summary>
        /// True is Sigma was built using another version of itself, false if
        /// it was built by Visual Studio. 
        /// </summary>
        public const bool IsMetaBuilt = false;

        public 
    }
}
